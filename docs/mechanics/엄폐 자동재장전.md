# 엄폐 자동재장전 동작 시나리오 (일반 메커니즘)

> 모드: 즉석 (특정 캐릭터 아님 — RL/SR 엄폐 자동재장전 메커니즘)

## 기본 정보
- 메커니즘: 풀차지 발사 후 `post_fire_delay` 동안 엄폐자세로 돌아가는 니케는, **재장 속도 ≥ 100%(재장 0초)**
  이면 딜레이 중 자동재장전이 완료되어 **최대장탄수가 유지**됨.
- 대상 판정: **CDN 유도** (2026-08-27부터. 종전에는 손으로 찍은 15명 목록이었다)

  ```
  cover_during_delay = input_type == "UP" and maintain_fire_stance == 0
  ```

  RL/SR 78명 중 **`false`는 9명뿐**이고 나머지 69명이 `true`다.

  | `false` 사유 | 캐릭터 |
  |---|---|
  | `DOWN_Charge` — 차지가 차면 자동 발사, 공격 사이에 엄폐 자세를 안 거친다 | 아니스 : 스타, 네온 : 비전 아이, 라플라스 : 얼티밋 히어로, 리버렐리오, 신데렐라, 베스티 : 택티컬 업(미등록) |
  | `maintain_fire_stance > 0` — 발사 후 사격 자세를 유지해 엄폐가 안 끊는다 | 레이븐, 홍련 : 흑영, A2(미등록) |

  유도식·근거·단위는 `docs/mechanics/CDN 발사 데이터.md`가 정본이다.
- 데이터: `data/parsed_nikke.json`의 `input_type` · `fire_stance_hold`.
  `data/weapon_delays.json`의 `_exceptions.<이름>.cover_during_delay`를 적으면 여전히 유도값을
  이기지만, 현재 등록된 예외는 없다(유도값이 관측 22건에 전부 맞는다).
- 구현: `calculator/timeline.py` `CharState._tick_charge` → `_auto_reload`.
- **엄폐컨(유저 조작)은 이 플래그와 무관하다** — `false`인 9명도 엄폐 버튼은 언제나 먹는다.
  막히는 건 딜레이 중 **자동으로** 엄폐 자세를 거치는 동작뿐이다 (유저 확인).

## 검증 스쿼드
- **표준**: `["아니스 : 스타", "크라운", "신데렐라", "헬름", "프리바티"]`, 큐브 재장 15레벨.
  - 풀버스트 중 재장 ≥100% (재장 0초), 풀버스트 밖 재장 <100% (0초 아님)인 조합.
  - 아니스 : 스타·신데렐라 = 비엄폐(대조군), 헬름 = 엄폐(대상).
    둘 다 `DOWN_Charge`라 비엄폐인 것이 CDN·유저 확인 양쪽에서 재확인됐다 — 대조군으로 유효하다.

## 동작 규칙

| 상황 | 엄폐 니케 (헬름) | 비엄폐 니케 (아니스 : 스타·신데렐라) |
|---|---|---|
| 재장 ≥100% (풀버스트) | 매 사격 후 딜레이 중 자동재장전 → 장탄 최대 유지 | 탄 전부 소모 후 재장전 (현행) |
| 재장 <100% (풀버스트 밖) | 탄 소모 후 재장전 (자동재장전 없음) | 탄 소모 후 재장전 |
| `squad_ammo_consume` | 매 사격 발동 | 매 사격 발동 |
| `event:full_reload` | 매 사격 발동(자동재장전 완료) | 탄 소진 후 재장전 시 발동 |
| `last_bullet` | **미발동** (장탄이 항상 최대라 ammo==1 미성립) | 탄 소진 직전 발동 |

## 경계 동작
- **풀버스트 진입 시점에 마지막 탄환을 장전 중이면** 그 1발은 정상 발사되어 `last_bullet`이 1회 발동한 뒤,
  이후 딜레이부터 자동재장전이 시작됨. 정상 상태(전입 후 첫 자동재장전 이후)에서는 `last_bullet` 미발동.
  → 물리적으로 충실한 동작 (그 순간 약실의 탄을 쏘는 것). 매 풀버스트가 아니라 진입 시 장탄 위상에 따라 가끔 발생.

## impl/bug-fix용 검증 체크리스트
- [ ] 헬름: 풀버스트 정상 구간 동안 장탄이 매 사격마다 최대치로 유지 (소진 후 0 패턴 사라짐).
- [ ] 헬름: 매 사격마다 `event:full_reload` 발동 (reload_log "자동 재장전(엄폐)").
- [ ] 헬름: 풀버스트 정상 구간 `last_bullet`(진두지휘 normal_atk_crit_rate) 미발동.
      단 풀버스트 진입 시 약실에 마지막 탄이 있으면 진입 직후 1회 발동 가능 (경계 동작).
- [ ] 헬름: 풀버스트 밖(재장 <100%)에서는 기존대로 탄 소진 후 재장전.
- [ ] 신데렐라(비엄폐): 동작 불변 — 탄 소진 후 재장전, `event:full_reload`로 무결한 유리 제거 유지.
- [ ] 사이클 간격 ~12.5s.
- [ ] 회귀 5/5 PASS (엄폐 플래그 캐릭터가 재장 100%에 도달하는 스쿼드가 없어 영향 없음).
