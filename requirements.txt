# 계산기 코어와 검증 하네스는 **표준 라이브러리만** 쓴다.
# CI가 이 파일을 설치하지 않는 것도 그래서다 — 아무것도 안 깔고 통과한다는 사실
# 자체가 "코어는 stdlib 전용"의 보증이 된다. 코어에 외부 의존성이 스며들면 CI가
# 즉시 깨져서 알려준다.
#
# 아래는 독립 분석 스크립트 하나(`context/accuracy_analysis.py`)만 쓴다.
numpy
