# 이 사이트가 다루는 데이터

`https://nikkedeck.tetra-pantone.ts.net` 운영 기준. 코드로 확인한 사실만 적는다 —
바뀌면 이 문서도 같이 고친다.

## 한 줄

**육성 데이터의 정본은 방문자 브라우저의 localStorage다.** 서버가 디스크에 쓰는 것은
**하나뿐**이다 — 직접 «공유 링크»를 눌러 만든 편성 공유본(편성 + 딜 수치, 24시간).

## 브라우저에 남는 것

전부 `localStorage`이고, 사이트 데이터를 지우면 같이 사라진다.

| 키 | 내용 |
|---|---|
| `nikke.profiles.v1` | 육성 스펙 (니케별 돌파·코강·스킬·장비·애장품·큐브, 콘솔, 동기화 레벨) |
| `nikke.decks.v2` | 덱 편성과 니케별 컨트롤 설정 |
| `nikke.results.v2` | 계산 결과 캐시 |
| `nikke.records.v1` | 저장한 기록 |
| `nikke.presets.v1` | 저장한 편성 프리셋 (편성 + 컨트롤 + 약점 코드·전투 조건) |
| `nikke.settings.v2` | 위크포인트·전투 시간·필터·즐겨찾기·전투 조건 |

서버로 보내지 않고도 전부 동작한다 — 계산을 「브라우저」로 두면 Pyodide가 같은 계산기를
브라우저 안에서 돌린다.

## 서버가 하는 일

`web/server.py` 하나가 전부다. **디스크에 쓰는 것은 공유 링크 하나뿐이다** — 프로필도,
조회 결과도, 접속 기록도 남기지 않는다.

### `POST /api/fetch` — 블라링크 조회

- 받는 것: 프로필 URL 또는 openid. `openid_from_input()`이 **숫자 openid만** 뽑아낸다.
- 하는 것: 블라링크 API에서 원본을 받아 그대로 돌려준다. 변환은 브라우저가 한다.
- **캐시하지 않는다.** 방금 육성한 것을 다시 불러도 옛 값이 나오면 안 되기 때문이다.
- 응답을 저장하지 않는다. 요청이 끝나면 메모리에서 사라진다.

> 이 조회는 **운영자 계정 세션**(`scraper/.session_cookie`)으로 나간다. 방문자 계정이
> 아니다. 그 대신 조회 대상은 「니케 정보 공개」를 켜 둔 계정만 가능하다 — 비공개
> 계정은 블라링크가 `1301002`로 막는다. 그 경우 북마클릿을 쓰면 된다(방문자 자기
> 브라우저에서 받으므로 공개 설정이 필요 없다).

### `POST /api/sim` — 계산

- 받는 것: 덱 이름 목록 + 육성 스펙 + 전투 조건.
- 계산이 **끝나는 즉시 육성 스펙을 버린다** (`_sim_worker`의 `j["payload"] = None`).
- 남는 것은 결과 숫자(총딜·니케별 딜·히트 수)뿐이고, 그것도 **15분 뒤 삭제**된다
  (`JOB_TTL`). 그 사이 결과를 보려면 작업 id를 알아야 한다.

### `POST /api/share` — 편성 공유 링크

**«공유 링크» 버튼을 눌렀을 때만** 만들어진다. 페이지를 열거나 계산하거나 기록을
저장하는 것만으로는 아무것도 올라가지 않는다.

- 올라가는 것: 덱별 니케 **이름**, 덱별·니케별 **딜 수치**, 약점 코드, 전투 시간.
- **올라가지 않는 것**: 육성 스펙, 계정 이름(`profileName`), 스펙 지문(`profileSig`),
  니케별 히트·크리 내역, 컨트롤 설정, 그리고 **기본 스펙 이탈 목록(`notes`)** —
  여기에는 `equip_skills.charge_speed_pct: 0 → 9.26`처럼 장비 실수치가 문장으로 들어
  있어서 일부러 뺀다.
- 서버가 받은 것을 **화이트리스트로 다시 짓는다**(`share_clean()`). 위에 없는 키는
  무엇을 보내든 저장되지 않는다.
- 저장 위치: `/var/lib/nikke-decklab/share.db` (sqlite 한 파일, `StateDirectoryMode=0700`).
  본문은 zlib 압축 JSON이고 5덱이 1.5KB 안쪽이다.
- **24시간 뒤 사라진다**(`SHARE_TTL`). 만료분은 다음 공유가 만들어질 때 지워진다.
- 코드는 `secrets.token_urlsafe(6)`(8자)다. 순번이 아니라서 남의 공유를 훑을 수 없다.
- 만든 사람은 링크 상자의 «링크 삭제»로 즉시 지운다(`POST /api/unshare`).
  코드를 아는 것이 곧 권한이다 — 계정이 없으므로 다른 소유 증명이 없다.
- 만드는 쪽(`POST`)은 **이 사이트 페이지에서 온 요청만** 받고 창당 6건으로 조인다.
  읽는 쪽(`GET`)은 링크를 받은 사람이 열어야 하므로 제한하지 않는다.

받는 사람이 가져가는 것은 **편성뿐**이다. 딜 수치는 보여 주기만 하고, 가져온 덱은
컨트롤 없이 «전부 자동»으로 들어간다.

### 메모리에만 잠깐 있는 것

| 무엇 | 얼마나 |
|---|---|
| 계산 중인 육성 스펙 | 그 계산이 도는 동안만 |
| 계산 결과 숫자 | 15분 (`JOB_TTL`) |
| 레이트리밋 카운터 (IP + 시각) | 60초 (`RATE_WINDOW`) |

서버를 재시작하면 전부 사라진다. 재시작해도 잃을 게 없는 구조다 — 공유 링크만
파일이라 재시작·재배포를 넘어 남고, 그것도 24시간이면 사라진다.

## 로그에 남는 것

systemd journal에 **`/api/` 요청만** 한 줄씩 남는다 — **주소는 적지 않는다**:

```
"POST /api/fetch HTTP/1.1" 200 -
```

- 방문자를 구분하지도, 기록하지도 않는다. IP도 세션 id도 남기지 않는다.
- openid는 POST **본문**에 있어서 요청줄에 안 남는다.
- 육성 데이터·계산 결과는 로그에 쓰지 않는다.
- 정적 파일(페이지·이미지) 요청은 아예 로그하지 않는다.

예상 못 한 오류가 나면 스택 트레이스가 journal에 남는다. 밖으로는 「서버 오류입니다」만
나간다 — 경로 같은 내부 정보를 응답에 싣지 않기 위해서다.

## 남용을 어떻게 막나

방문자를 구분하지 않기로 했으므로 «누가 많이 썼나»로는 막을 수 없다. 대신 서버가
**자기가 밖으로 거는 호출**을 조인다.

- 블라링크 호출 사이에 **1.5초 간격**을 둔다(`API_GAP`). 조회 한 번은 요청 6건이라
  약 9초가 걸린다. 몰리면 거절하지 않고 차례를 기다린다.
- 줄이 30초 넘게 밀리면 그때만 「잠시 후 다시」로 돌려보낸다.
- `/api/fetch`는 **이 사이트 페이지에서 온 요청만** 받는다(`Sec-Fetch-Site` 검사).
  크롤러와 스크립트가 여기서 걸린다.
- `robots.txt`와 `<meta name="robots">`로 검색·AI 수집을 거부한다. 다만 이건 부탁이지
  자물쇠가 아니다 — 실제 차단은 위의 둘이 한다.

그 대가로 **한 사람이 몰아 쓰면 다른 사람도 같이 느려진다.** 구분할 근거를 두지
않기로 한 결과이고, 그 편이 낫다고 판단했다.

## 제3자로 나가는 것

| 어디로 | 무엇이 | 누가 |
|---|---|---|
| `cdn.jsdelivr.net` | Pyodide(파이썬 런타임) 내려받기 | 브라우저. 「브라우저 계산」을 쓸 때만 |
| `api.blablalink.com` | openid 하나 | 서버. `/api/fetch`를 쓸 때만 |

니케 초상화·아이콘은 **이 서버에서** 준다. 외부로 나가지 않는다
(CSP `img-src 'self' data: blob:`가 이를 강제한다).

분석 도구·광고·추적기는 없다. 쿠키를 굽지 않는다.

## 지우는 법

- **내 스펙**: 「내 계정」 탭 → 스펙 카드의 「삭제」
- **전부**: 브라우저 사이트 데이터 삭제 (localStorage가 정본이라 이걸로 끝난다)
- **내가 만든 공유 링크**: 링크 상자의 「링크 삭제」. 안 지워도 24시간 뒤 사라진다.
- 그 밖에 서버에는 지울 것이 없다 — 저장하지 않는다.

## 직접 확인하려면

| 주장 | 확인 |
|---|---|
| 쓰기 경로가 하나뿐 | 유닛의 `ProtectSystem=strict` + `StateDirectory=nikke-decklab` |
| 공유본에 스펙이 없다 | `share_clean()`의 화이트리스트 (`web/server.py`) |
| 공유는 눌렀을 때만 | `makeShare()`를 부르는 곳이 «공유 링크» 버튼뿐이다 (`web/src/app.js`) |
| 공유가 24시간 | `SHARE_TTL` + `share_put()`의 `DELETE WHERE created < …` |
| 조회를 캐시 안 한다 | `fetch_cached()`가 언제나 `(raw, False)`를 준다 |
| 계산 뒤 스펙을 버린다 | `_sim_worker()`의 `j["payload"] = None` |
| 15분 뒤 결과 삭제 | `JOB_TTL` + `_gc_jobs()` |
| 로그에 본문 없음 | `Handler.log_message()` |

## 다른 사람 계정을 조회하는 것에 대해

URL 조회는 **공개로 설정된 프로필**만 볼 수 있다. 이것은 블라블라링크가 그 계정 주인의
설정에 따라 이미 공개하고 있는 정보이며, 이 사이트는 그것을 계산에 쓸 뿐 저장하지
않는다. 공개를 원치 않으면 블라블라링크 설정에서 「니케 정보 공개」를 끄면 여기서도
조회되지 않는다.
