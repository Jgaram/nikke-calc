# 아래 경로가 바뀌면 @Jgaram에게 자동으로 리뷰 요청이 간다.
#
# **머지를 막지 않는다.** 브랜치 보호의 `Require review from Code Owners`는 일부러
# 꺼 두었다 — 켜면 @Jgaram이 자리를 비운 동안 이 경로의 변경이 영구히 멈춘다.
# 여기서 하려는 것은 차단이 아니라 알림이다.
#
# 왜 이 둘인가: 게임 데이터와 golden snapshot은 코드 리뷰로 검증할 수 없고
# **인게임 실측**이 있어야 한다. 그래서 알고 지나가야 할 경로로 표시해 둔다.
# 나머지 전 영역은 CI 초록불이면 그대로 머지해도 된다.

/data/               @Jgaram
/baseline/           @Jgaram
